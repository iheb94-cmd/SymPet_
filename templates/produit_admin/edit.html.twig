{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}

    <div class="container mt-4">

        <h2>Edit Produit</h2>

        <div class="card">
            <div class="card-body">

                {{ include('produit_admin/_form.html.twig', {'button_label': 'Update'}) }}

            </div>
        </div>

        <div class="mt-3 d-flex gap-2">

            <a href="{{ path('app_produit_admin_index') }}"
               class="btn btn-secondary">
                Back
            </a>

            {{ include('produit_admin/_delete_form.html.twig') }}

        </div>

    </div>

{% endblock %}
