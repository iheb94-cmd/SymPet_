{% extends 'base.html.twig' %}

{% block body %}

    <div class="container mt-4">

        <h2>Edit catégorie</h2>

        <div class="card">
            <div class="card-body">

                {{ include('categorie_admin/_form.html.twig', {'button_label': 'Update'}) }}

            </div>
        </div>

        <div class="mt-3 d-flex gap-2">

            <a href="{{ path('app_categorie_admin_index') }}"
               class="btn btn-secondary">
                Back
            </a>

            {{ include('categorie_admin/_delete_form.html.twig') }}

        </div>

    </div>

{% endblock %}
